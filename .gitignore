# Claude
.claude/

# OS / editor
.DS_Store

# Dependencies / builds
node_modules/
dist/
target/

# Local-only admin design preview (renders the real UI with mocked API data).
# Kept at the repo root, NOT src-ui/entries/, so check:architecture skips it.
/admin-preview.html
/admin-preview.js

# Env files
.env
.env.*
!.env.example

# Local playback/cache artifacts
cache/
tmp/

# Large local media files
*.mkv
*.webm
*.avi
*.mov
*.m4v
# MPEG-TS video segments (not TypeScript)
*.m2ts
*.mts
*.mp4
assets/videos/
